---
description: LLM provider implementation patterns
globs: src/llm/**
---
- Providers implement `LLMProvider` in `src/llm/types.ts`.
- Configured via environment variables or file in `src/llm/config.ts`.
- Seat-based options include `cursor-acp.ts` and `claude-cli.ts`.